# Lets `pytest` work from the repo root, not just from backend/.
# Without this, root pytest never discovers backend/pyproject.toml and 10 async
# tests fail with "async def function and no async plugin installed".
[pytest]
testpaths = backend/tests
asyncio_mode = auto
