[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "scrapesmith"
version = "0.1.0"
description = "Self-healing HTML parser — spike + app skeleton"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "anthropic>=0.40",
    "playwright>=1.44",
    "beautifulsoup4>=4.12",
    "lxml>=5.2",
    # app skeleton (Phase 0.5)
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.29",
    "alembic>=1.13",
    "arq>=0.26",
    "pydantic-settings>=2.3",
    # already present transitively via pydantic-settings; declared because config.py
    # imports load_dotenv directly, and a transitive dep is not a contract.
    "python-dotenv>=1.0",
    "python-multipart>=0.0.9",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2",
    "pytest-cov>=5.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.5",
]

[tool.setuptools.packages.find]
where = ["."]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py312"
line-length = 100
# spike/ is the eval harness (Phase 0 de-risk rig, now the heal bench) and keeps its own
# pre-3.9 typing style; alembic/versions is autogenerated.
extend-exclude = [".venv", "alembic/versions", "spike"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
# B008: FastAPI Depends() in defaults is idiomatic.
ignore = ["B008"]
