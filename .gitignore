# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node / Next
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env
.env
.env.*
!.env.example
!.env.local.example

# DB / artifacts
*.sqlite
*.sqlite3
data/
uploads/
# (contents, not the dir itself — git cannot re-include a file under an excluded dir)
**/artifacts/*
# ...except the committed heal-bench baseline: the README quotes its number, so the
# evidence for that number has to be in the repo next to it.
!backend/artifacts/phase0_report.json
!backend/artifacts/phase0_report.md

# Editor
.DS_Store
.idea/
.vscode/

# Local Claude state
.claude/sessions/

# tool caches
.serena/
